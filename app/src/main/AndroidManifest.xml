<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.saulmm.material">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo2"
        android:label="Takshak&apos;16"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.Main"
            android:theme="@style/TransitionSampleActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.Registration"
            android:theme="@style/TransitionSampleSecondActivity" />
        <activity
            android:name=".myapplication.RegistrationMain"
            android:theme="@style/TransitionSampleSecondActivity" />
        <activity android:name=".myapplication.ViewReg" />
        <activity android:name=".activities.Events" />
        <activity android:name=".myapplication.qrcode"></activity>
    </application>

</manifest>
